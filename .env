# Template — placeholder values only, safe to commit. Real values go in
# .env.local (gitignored, never committed) or as environment variables in
# your Vercel project settings.

# Supabase Postgres. In the Supabase dashboard: Project Settings > Database
# > Connection string.
#
# DATABASE_URL must be the "Transaction" pooler URI (port 6543, with
# ?pgbouncer=true) — this is the runtime connection and the only one that
# survives serverless concurrency. Do NOT point it at the session pooler
# (5432) because that benchmarks faster on a single query: session mode holds
# one Postgres connection per client and Supabase caps that pool at 15, so two
# concurrent lambdas take the whole site down with EMAXCONNSESSION. That
# happened; see BUILD_LOG.md.
#
# DIRECT_URL is the "Session" pooler URI (port 5432, same host) — used only
# for migrations, which need a non-pooled connection and run one at a time.
#
# Keep the database in the SAME region as the Vercel deployment (pinned to
# bom1/Mumbai in vercel.json). Every query pays the round-trip between them,
# and pages issue several — a mismatched pair cost ~890ms per query and made
# the whole app feel broken. Co-located, it's ~27-126ms. See BUILD_LOG.md.
DATABASE_URL="postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=5"
DIRECT_URL="postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-ap-south-1.pooler.supabase.com:5432/postgres"

# Auth.js — generate a real secret with: npx auth secret
AUTH_SECRET="replace-with-a-real-secret"
NEXTAUTH_URL="http://localhost:3000"

# Google Cloud OAuth app (Gmail + Calendar APIs enabled). See
# BUILD_LOG.md for the step-by-step setup.
GOOGLE_CLIENT_ID="replace-with-your-client-id"
GOOGLE_CLIENT_SECRET="replace-with-your-client-secret"

# Optional — AI features degrade gracefully without this.
OPENAI_API_KEY=""
